schema: kb.agent/1
id: test-agent-1
name: Test Agent One
description: First test agent for unit tests

metadata:
  description: "Test agent for monorepo tasks. Can check and validate."
  tags: [test, monorepo, validation]
  examples:
    - "Check something"
    - "Validate structure"

llm:
  tier: small
  temperature: 0.0
  maxTokens: 1000
  maxToolCalls: 5

prompt:
  systemPrompt: "You are a test agent."

tools:
  filesystem:
    enabled: true
    mode: allowlist
    allow: ["fs:read"]

policies:
  allowWrite: false
