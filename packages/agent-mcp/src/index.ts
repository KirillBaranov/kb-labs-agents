// MCP Client — connects to MCP servers, exposes tools as MCPToolPack
export * from './client/index.js';

// MCP Server — exposes the KB Labs Agent as an MCP endpoint
export * from './server/index.js';
